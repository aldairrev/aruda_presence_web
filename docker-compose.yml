services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aruda_presence_prod
    ports:
      - "${APP_PORT:-80}:8000"
    env_file:
      - .env
    networks:
      - backend
    restart: always

networks:
  backend:
    external: true
