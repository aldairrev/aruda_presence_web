services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aruda_presence_prod
    ports:
      - "${APP_PORT:-80}:8000"
    volumes:
      - ./.env:/var/www/html/.env
    networks:
      - backend
    restart: always

networks:
  backend:
    external: true
